## 简明讲稿与代码/流程图三位一体指引

老师要求在讲解流程图时穿插展示对应的 Java 代码。为了使您的讲解更加自然流畅，我们破除了原先按流程图死板划分的 19 个独立标题和流水账式过渡语，将讲稿重构为四大逻辑模块。

在讲稿中，所有的代码跳转指引和口语化讲解词均已加粗。您在答辩念稿时，念到加粗位置即可迅速点击跳转至对应的 Java 代码，然后顺着说出讲解词，即可实现行云流水的演示效果。

---

### 一、 系统架构与底层数据支撑

**【显示流程图：1-life.vsdx】** 在系统生命周期的拉起阶段，程序的入口非常轻量，仅仅负责把进程托管给 JavaFX 框架，**【跳转 [App.java:L6-L7](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/App.java#L6-L7) ✦ 讲解词：“大家可以看到，App 启动的主入口非常简洁，仅仅调用了应用程序的启动方法，将控制权转交，以此拉开整个窗口生命周期的序幕。”】** 随后系统所有的窗口、页面和后端初始化工作都从主窗口类的启动函数开始运行。在页面初始化过程中，系统会首先在控制器中完成接收确认和进度更新的回调方法注册，**【跳转 [MainWindow.java:L117-L125](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L117-L125) ✦ 讲解词：“在此处，主窗口启动时会注册文件接收询问和进度更新的监听回调，让后端的网络事件能够安全地通知界面线程进行刷新。”】** 紧接着在创建后端服务类时，后端会自动在构造方法中启动接收监听并并发加载本地的系统设置。**【跳转 [LocalBackend.java:L25-L28](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L25-L28) ✦ 讲解词：“在本地后端服务的构造方法中，我们通过调用接收服务的启动函数，在后台开启了一个独立的守护线程，负责持续监听 UDP 文件包。”】** 

**【切换流程图：2-storage.vsdx】** 为了避免多实例在同一台机器联调或运行时发生冲突，系统在本地设计了统一的数据目录，该目录会优先使用启动参数或环境变量来覆盖，在默认情况下则会定位到用户的主目录下。**【跳转 [AppFiles.java:L13-L29](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AppFiles.java#L13-L29) ✦ 讲解词：“在路径工具类中，我们读取配置参数或环境变量来动态决定数据目录，默认落在用户目录下的专属隐藏文件夹中，以保证多实例运行的数据独立性。”】** 系统的设置信息、近期发送对象、群组以及记住的账号，都是通过属性文件读取并由专门的存储服务分别读写的，**【跳转 [SettingsStore.java:L38-L65](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/SettingsStore.java#L38-L65) ✦ 讲解词：“以配置加载为例，我们从磁盘的属性文件中读写数据，且相关读写方法均使用了线程同步锁，确保多线程并发环境下的文件读写安全。”】** 

**【切换流程图：3-model.vsdx】** 并且为了规范界面和后端之间的交互，所有的个人资料、系统设置以及传输的任务汇总均由专门的不可变模型对象承载，使得数据在不同层之间流动时具备高度的一致性和安全性。**【跳转 [SystemSettings.java:L5-L13](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/model/SystemSettings.java#L5-L13) ✦ 讲解词：“例如系统设置这个数据模型，我们使用了只读的对象声明，在内存中确保了配置数据的只读不可变属性，极大地提升了业务数据的稳定性。”】**

---

### 二、 安全凭证与用户个性化配置

**【切换流程图：4-login.vsdx】** 当底层的数据与配置存储准备就绪后，用户即可通过界面进行注册与登录操作。在登录页加载时，页面会先从本地记住账号的文件中读取并回填账号，**【跳转 [Auth.java:L80-L86](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Auth.java#L80-L86) ✦ 讲解词：“在登录页面初始化时，程序会异步加载先前保存的登录名并自动回填输入框，省去用户重复输入的麻烦。”】** 用户提交登录请求后，请求会穿过控制器进入后端的安全校验逻辑，后端通过读取数据表，提取出该用户专属的盐值与哈希，进行高强度的本地密码摘要比对。**【跳转 [AuthStore.java:L65-L84](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L65-L84) ✦ 讲解词：“在登录验证逻辑中，我们从磁盘读取属性账密文件，利用盐值和哈希比对密码，使得我们在没有中央数据库服务器的情况下也能实现安全的密码验证。”】** 此处的密码哈希计算使用了带盐的复杂迭代摘要算法，在客户端完成多达十二万次的哈希计算以抵御彩虹表与暴力破解攻击。**【跳转 [AuthStore.java:L386-L394](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L386-L394) ✦ 讲解词：“密码哈希具体的算法使用了十二万次循环加密，在客户端对用户密码进行加盐哈希，大大增强了本地账密数据的防护强度。”】** 

**【切换流程图：5-reg.vsdx】** 而如果是新用户进行注册，为防止在网络较慢时发生重复提交，注册表单在点击提交后会立即禁用所有输入控件并显示加载动画，**【跳转 [Auth.java:L123-L128](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Auth.java#L123-L128) ✦ 讲解词：“在注册按钮点击后，我们将输入框与提交按钮全部挂起禁用，防止多次快速连击导致提交多份注册申请。”】** 且因为该项目没有独立的中央服务器，系统会通过运行时注入的辅助账号临时凭证，以自动化推送的方式把注册请求推送到 GitHub 远程仓库，并由 GitHub Actions 的流水线自动将其合入主账号表。**【跳转 [AuthStore.java:L99-L116](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L99-L116) ✦ 讲解词：“在注册逻辑中，如果开启了 Git 同步，程序会生成待审核的注册文件，并使用辅助凭证安全拼装推送地址，通过命令行提交推送到 GitHub，省去了资金购买和维护服务器，刚好。”】** 

**【切换流程图：6-prof.vsdx】** 登录成功后，用户可以在资料维护页面修改展示信息，例如上传头像时，系统会使用图形工具包对原始图片进行正方形裁剪，并压缩缩放到标准的百像素大小以缩减网络分发的开销。**【跳转 [Mine.java:L132-L146](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Mine.java#L132-L146) ✦ 讲解词：“在处理上传头像时，程序提取图片的最短边并进行正方形居中裁剪，生成 128 像素的缩略图，以此降低局域网内扫描广播的传输消耗。”】** 随后将头像转换为 Base64 编码的文本随同昵称和签名一并保存，并通过回调实时同步给局域网发现和 UDP 接收状态。**【跳转 [Mine.java:L147-L158](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Mine.java#L147-L158) ✦ 讲解词：“紧接着，我们通过格式输出器将裁剪后的图像进行有损压缩，转换成 Base64 编码的文本存储，以便后续在局域网广播发现中直接携带。”】** 

**【切换流程图：7-set.vsdx】** 同时，系统设置的修改（如上传下载限速、重试次数等）也会实时持久化到本地设置文件中，并且当用户更新了用于局域网隔离的口令组后，系统后端还会同步更新局域网口令摘要以实现群组隔离。**【跳转 [Settings.java:L217-L247](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Settings.java#L217-L247) ✦ 讲解词：“用户保存设置后，不仅会将配置写入磁盘，还会触发界面的重绘渲染方法，动态改变主题颜色或缩放，不需要重启程序即可生效。”】** 

**【切换流程图：8-tray.vsdx】** 此外，这些设置还会影响自启动脚本在系统自启动文件夹中的生成，并且在登录完成后根据配置决定是否直接隐藏到系统托盘，利用底层的系统托盘工具实现无缝的后台驻留服务。**【跳转 [MainWindow.java:L413-L450](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L413-L450) ✦ 讲解词：“我们在主窗口中集成了托盘逻辑，使用底层的系统托盘及原生右键菜单，让程序能直接隐藏至任务栏托盘，在后台静静运行接收文件。”】** 特别是在传输或接收完成时，如果用户开启了提示音，程序还会调用硬件工具包播放提示音给出声音反馈。**【跳转 [MainWindow.java:L1251-L1258](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L1251-L1258) ✦ 讲解词：“当长耗时的传输完成，如果配置中的提示音选项为真，就会使用系统级的底层喇叭发出蜂鸣提示音，向用户提供直观的声音反馈。”】**

---

### 三、 拓扑扫描与关系管理

**【切换流程图：9-userui.vsdx】** 在软件的基础配置和安全通道建立好之后，传输流程的核心就在于寻找局域网内的接收设备。用户列表页面在初始化时会并行加载已发现的在线用户以及本地持久化的自定义分组。**【跳转 [UserList.java:L44-L52](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/UserList.java#L44-L52) ✦ 讲解词：“在用户列表加载时，我们并发获取所有已在线 of 局域网设备以及本地定义的分组，通过异步任务编排确保获取过程不阻塞主线程。”】** 

**【切换流程图：10-scan.vsdx】** 为了获取到真实的物理设备信息，当用户点击扫描时，后端模块会启动扫描流程，利用网络套接字向回环地址、局域网全局广播地址以及每一块活动网卡的子网广播地址并发广播发现包。**【跳转 [LanPeer.java:L69-L88](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LanPeer.java#L69-L88) ✦ 讲解词：“在局域网扫描方法中，我们设置了套接字读取的超时时限，并将特定的发现请求同时广播发往本地回环以及所有物理网卡所在的子网广播地址，收集周围的应答反馈。”】** 此时局域网内所有处于活跃状态的接收端会在其常驻的监听端口收到该广播包，在验证对方的口令组哈希匹配并且自身不处于隐身或离线状态后，会直接以单播形式向发起端回复包含自身名片信息的应答包。**【跳转 [LanPeer.java:L156-L180](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LanPeer.java#L156-L180) ✦ 讲解词：“应答方的后台监听循环接收到广播包后，核对是否符合群组口令。若符合且自身状态允许被发现，则将当前配置打包通过单播发送回去。”】** 

**【切换流程图：11-group.vsdx】** 发起端在收集到这些应答后会实时更新本地的设备状态列表。为了方便进行批量管理与批量发送，用户还可以通过多选这些设备并为它们设定一个组名与默认口令，将其保存为一个局域网群组，其成员关系快照将持久化记录在本地分组属性文件中。**【跳转 [GroupStore.java:L33-L43](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/GroupStore.java#L33-L43) ✦ 讲解词：“在分组保存时，我们清洗掉无效数据，将新组写入分组配置文件中，在逻辑层面上形成一个虚拟的组目标以供界面选择。”】** 

**【切换流程图：12-recent.vsdx】** 在文件传输页面打开时，系统会进一步将本地群组目标与近期发送的历史对象列表进行加载与去重合并，并将其按最近使用时间呈现给用户，而在一次文件发送任务真正下发前，后端服务还会调用展开逻辑将选中的虚拟群组对象重新解构展开为具体的真实用户列表，实现一键多设备批量发送。**【跳转 [GroupStore.java:L72-L93](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/GroupStore.java#L72-L93) ✦ 讲解词：“在数据发送前，我们将用户选择的发送目标解构展开，遍历将所有分组目标还原为其对应的真实分组成员，以便在发送端独立建立多条单播通道。”】**

---

### 四、 多线程UDP传输与可靠性闭环

**【切换流程图：13-fileprep.vsdx】** 当接收目标确定且待传输的文件和文件夹就绪后，系统即进入了实际的文件流转和传输控制阶段。首先，界面会对拖拽或选中的文件进行过滤，阻断不支持的文件格式，并在发送前通过递归遍历算法将文件夹完全平展为多个独立的子文件任务，同时在发送端计算每个文件的 SHA-256 散列哈希。**【跳转 [UdpTx.java:L602-L634](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L602-L634) ✦ 讲解词：“在收集发送源文件时，如果是文件夹，我们利用文件树递归遍历子目录，清洗提取出所有支持的文件并构建路径，在此过程中计算每个文件的散列哈希值，作为传输校验的依据。”】** 

**【切换流程图：14-txui.vsdx】** 在用户提交发送并完成口令校验后，该发送任务将进入本地后端的单线程队列进行排队执行，以串行方式调度以防止多线程抢占网卡而引发网络严重拥堵。**【跳转 [LocalBackend.java:L18-L22](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L18-L22) ✦ 讲解词：“我们设计了单线程的任务执行队列，所有的传输请求都在这里串行化排队发送，避免多个发送任务竞争物理带宽而造成局域网丢包率剧增。”】** 

**【切换流程图：15-txprep.vsdx】** 发送器启动后，会先向接收端投递一个携带了文件名、哈希、分片信息的开始包引导报文，接收端收到该报文后会先进行门禁检查。**【跳转 [UdpTx.java:L255-L269](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L255-L269) ✦ 讲解词：“在数据发送前，我们会先向对方发出一个引导数据包，通过 UDP 发送引导包元数据来告知对方文件长度、总片数、以及安全哈希，以此建立可靠传输的前置握手。”】**

**【切换流程图：16-txchunk.vsdx】** 待引导阶段确认通过后，发送端便会启动 Worker 线程将文件分割为数万字节 of 固定分片进行高并发的 UDP 发送，并且在底层的确认应答循环中实时处理用户触发的发送暂停、根据设置限速控制发送频率以及对丢包分片进行指定次数的重传。**【跳转 [UdpTx.java:L452-L479](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L452-L479) ✦ 讲解词：“带确认的发送逻辑在此处实现。如果在循环中监测到暂停信号，发送线程将受阻暂停；如未收到接收方的确认，则会在配置的重试上限内重发当前分片。”】** 

**【切换流程图：17-rxbegin.vsdx】** 从接收端看，第一步是处理发送端的引导报文。如果接收方判定该文件曾经传输过一部分，则会在确认包中直接带回本地已收到的分片元数据以实现断点续传；**【跳转 [UdpRx.java:L123-L162](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpRx.java#L123-L162) ✦ 讲解词：“在接收方的开始包处理逻辑中，我们检查该任务。如果是由于网络中断未完成的历史任务，我们将本地保存的已接收片元元数据取回并响应返回，实现断点续传。”】** 而在新传输任务或接收方处于忙碌/口令保护状态下时，接收方的界面主线程还会安全地弹窗询问用户或进行口令校验。**【跳转 [MainWindow.java:L170-L180](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L170-L180) ✦ 讲解词：“如果接收方当前处于忙碌状态，接收逻辑将通过回调向主窗口提交弹窗，在界面线程异步等待 30 秒以供用户决定是否接收文件。”】** 

**【切换流程图：18-rxdata.vsdx】** 接收端在收到各个数据分片后，会使用文件随机访问通道按照对应的块索引将分片数据随机写入临时文件中，并实时保存分片接收 of 元数据，直至全部片元集齐后，接收端会重算文件的 SHA-256 散列值进行一致性核对，核对无误后才会通过文件移动操作将其重命名为最终文件并删除临时记录以保障文件完整可靠。**【跳转 [UdpRx.java:L371-L466](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpRx.java#L371-L466) ✦ 讲解词：“当所有分片接收齐备后，程序重算文件指纹。如果与引导包里的散列值匹配，就调用物理移动方法，将临时文件正式重命名，并物理删除临时状态元数据文件。”】** 

**【切换流程图：19-result.vsdx】** 最终，无论是正在进行还是已经完成的任务，页面均会依靠后端不断产出的任务状态摘要更新结果列表的各项统计与日志展现，且用户可以自由清空日志或清除已完成的任务以确保界面整洁，完成整个可靠传输的完美闭环。**【跳转 [FileTransfer.java:L229-L274](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/FileTransfer.java#L229-L274) ✦ 讲解词：“结果汇总页依据数据模型提供的信息来实时渲染。如果用户清空日志，程序会生成排除日志的摘要对象以避免引起多线程渲染的数据冲突。”】**
