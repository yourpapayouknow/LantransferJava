## 简明讲稿与代码行间穿插指引

老师要求在讲解流程图时穿插展示对应的 Java 代码。为此，我们在本讲稿中，将所有代码跳转指引和讲解词，以加粗的 `**【跳转 文件名.java:行号 ✦ 讲解词：“...”】**` 形式，直接插入到您要念的句子行间。

在答辩时，您只需要一边念稿，一边在念到加粗提示时切换到对应的 Java 文件和行号，并顺着说出讲解词，即可实现流程图与代码的无缝交互。

---

### 1. App生命周期流程：`life.vsdx`

我们先从整个App的生命周期讲起。程序入口本身很轻，`App.main`只负责把进程交给JavaFX，**【跳转 [App.java:L6](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/App.java#L6) ✦ 讲解词：“大家可以看到，App 类里的 main 方法极其简洁，只有一行 launch 命令，这直接把启动控制权托付给了 JavaFX 框架。”】** 真正的窗口、页面和后端初始化都从`MainWindow.start`开始。主窗口启动后，会先注册接收确认和进度回调，**【跳转 [MainWindow.java:L117-L122](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L117-L122) ✦ 讲解词：“在 MainWindow 启动的 start 方法中，首先通过 controller 注册了接收确认回调 confirmReceive 和进度回调 showRxProgress，让后端接收事件能安全通知界面。”】** 再创建`LocalBackend`，由后端启动UDP接收线程并加载系统设置。**【跳转 [LocalBackend.java:L25-L28](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L25-L28) ✦ 讲解词：“LocalBackend 构造时，会执行 rx.start()。这个方法会在后台开启独立的常驻线程，负责监听 UDP 文件传输，保证只要程序存活就能持续接收文件。”】** 用户登录失败时会显示错误并停留在登录页，登录成功以后进入主界面；**【跳转 [Auth.java:L89-L97](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Auth.java#L89-L97) ✦ 讲解词：“这是登录按钮的点击事件，一旦 controller.login 成功返回且 Profile 有效，我们就调用 showFileTransferPage 切入传输主界面，否则只弹出 Toast 提示并停留在原地。”】** 后续文件传输、用户列表、我的资料和系统设置都由侧边栏切换。最后用户关闭窗口或从托盘退出，进程才完成销毁。

对应Java文件：`App.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`SettingsStore.java`、`UdpRx.java`。

---

### 2. 本地数据与配置存储流程：`storage.vsdx`

理解了App怎么启动之后，接下来要说明它的数据放在哪里。这个项目没有把运行数据随意散落在工程目录里，而是由`AppFiles.dataDir`统一决定数据目录：**【跳转 [AppFiles.java:L14-L20](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AppFiles.java#L14-L20) ✦ 讲解词：“在 AppFiles 里，我们统一了数据存放策略。我们读取参数或环境变量以决定重定向，默认存放在用户 Home 目录的 .lantransfer 文件夹下，并以 Git 仓库名作为子目录以实现多实例隔离。”】** 优先使用启动参数或环境变量，默认落在用户目录下的`.lantransfer`。设置、近期对象、分组和记住账号分别由各自的Store读写；**【跳转 [SettingsStore.java:L38-L43](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/SettingsStore.java#L38-L43) ✦ 讲解词：“以 SettingsStore 的 load 方法为例，我们通过属性配置类读取 settings.properties 文件，读取方法都带有 synchronized 锁，保证了文件多线程读取的安全性。”】** 账号主表仍然使用仓库根目录的`acco`，注册请求写入`req/`。最后这些来源都由`LocalBackend`组合起来，供页面统一调用。

对应Java文件：`AppFiles.java`、`SettingsStore.java`、`RecentStore.java`、`GroupStore.java`、`AuthStore.java`、`LocalBackend.java`。

---

### 3. 核心数据模型承载关系：`model.vsdx`

有了数据落点以后，再看页面和后端之间传递的对象。页面层不直接操作文件、网络或账号表，而是通过`AppController`进入`BackendFacade`，再交给`LocalBackend`处理。登录结果、个人资料、系统设置、用户设备、分组、传输文件和传输结果都由明确的record或enum承载，**【跳转 [SystemSettings.java:L5-L10](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/model/SystemSettings.java#L5-L10) ✦ 讲解词：“比如配置模型 SystemSettings，我们大量使用了 Java 17 的 record 语法。它天然具备字段不可变性，使得数据在业务层和 UI 层之间流动时更为安全、一致。”】** 这样页面只关心展示和事件，后端只关心业务执行。`MockBackendFacade`和`TxSim`只用于联调或自检，不属于当前正式主流程。

对应Java文件：`AppController.java`、`BackendFacade.java`、`LocalBackend.java`、`AuthResult.java`、`LoginRequest.java`、`RegisterRequest.java`、`Profile.java`、`SystemSettings.java`、`UserDevice.java` swallow `Group.java`、`TransferFile.java`、`TransferTask.java`、`TransferSummary.java`、`UserStatus.java`、`DeviceStatus.java`、`MockBackendFacade.java`、`TxSim.java`。

---

### 4. 登录流程：`login.vsdx`

接下来进入用户真正开始使用App的第一步：登录。登录页会先从本机`la`里读取上次记住的账号，**【跳转 [Auth.java:L80-L86](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Auth.java#L80-L86) ✦ 讲解词：“登录表单加载时，页面会自动调用 controller.loadRememberedAccount 获取本地记住的历史账号，填入框中并默认勾选‘记住我’，改善用户体验。”】** 用户输入账号和密码后，页面把请求交给`AppController.login`，再进入`LocalBackend`和`AuthStore.login`。后端会读取`acco`账号表，**【跳转 [AuthStore.java:L65-L75](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L65-L75) ✦ 讲解词：“AuthStore.login 中会把账号做标准化，读取数据表中的 salt 盐值与预期哈希，核对密码。这种无服务器模式消除了重型服务端的开销。”】** 用盐和PBKDF2摘要校验密码；**【跳转 [AuthStore.java:L386-L394](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L386-L394) ✦ 讲解词：“密码哈希采用 PBKDF2WithHmacSHA256 算法，迭代 120,000 次，在客户端完成极高强度的加密验证，有效防止撞库和暴力破解。”】** 校验成功后生成`Profile`，保存记住账号状态，并把当前用户同步给局域网发现和UDP接收模块。也就是说，登录不只是换到主界面，它同时建立了后面扫描、接收和发送所需的身份。

对应Java文件：`Auth.java`、`AppController.java`、`LocalBackend.java`、`AuthStore.java`、`LoginRequest.java`、`AuthResult.java`、`Profile.java`、`LanPeer.java`、`UdpRx.java`。

---

### 5. 注册流程：`reg.vsdx`

如果用户还没有账号，就走注册流程。注册页提交账号、密码和设备名后，页面会先禁用表单并显示处理中，防止重复提交。**【跳转 [Auth.java:L123-L128](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Auth.java#L123-L128) ✦ 讲解词：“当用户按下注册按钮时，我们会立即禁用所有文本框和注册按钮本身，并拉起进度圈，防止网络延迟或用户狂点导致多次向仓库写入数据。”】** 后端校验账号格式和重复账号，再生成盐和PBKDF2密码摘要，把注册请求写入`req/<账号>`。因为当前没有独立服务器，App通过运行时注入的`ACCO_T`或`-Dacco.t`把请求推送到GitHub仓库，**【跳转 [AuthStore.java:L99-L116](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L99-L116) ✦ 讲解词：“在 AuthStore.register 中，保存注册配置到临时目录后，会通过运行时传入的 classic PAT 口令（token()）动态拼接带有安全认证的 HTTPS 推送路径，利用本地 Git 提交并推送该请求，免去了部署专属后台的麻烦。”】** 等待Actions把请求合入`acco`；如果没有及时合入，用户会看到审核等待状态。

对应Java文件：`Auth.java`、`AppController.java`、`LocalBackend.java`、`AuthStore.java`、`RegisterRequest.java`、`AuthResult.java`、`Profile.java`。

---

### 6. 我的资料流程：`prof.vsdx`

账号建立之后，用户可以在“我的资料”里维护自己的展示信息。`Mine`页面会先检查当前是否已有`Profile`，未登录就回到登录页；已登录时展示昵称、用户ID、设备名、签名和头像。头像上传后会用`ImageIO`裁剪压缩成128px JPEG，**【跳转 [Mine.java:L132-L146](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Mine.java#L132-L146) ✦ 讲解词：“在头像上传后，jpegAvatar 会把原始图片读取为 BufferedImage，寻找其最短边计算并进行居中裁剪，压缩缩放至 128 像素，保持极小的数据传输体量。”】** 再转成Base64保存。**【跳转 [Mine.java:L147-L158](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Mine.java#L147-L158) ✦ 讲解词：“接着调用 AWT 的 ImageWriter 把裁好的头像以 0.72 的品质因数转为 JPEG 格式，以 Base64 编码保存进属性表，用于局域网内的展示。”】** 资料和状态保存成功后，会写回账号数据，并同步给`LanPeer`广播和`UdpRx`接收门禁，这样别人扫描到的资料和接收端的状态判断都会更新。

对应Java文件：`Mine.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`AuthStore.java`、`Profile.java`、`UserStatus.java`、`LanPeer.java`、`UdpRx.java`。

---

### 7. 系统设置流程：`set.vsdx`

资料解决“我是谁”，系统设置解决“App怎么工作”。设置页会读取`SystemSettings`，展示本机IP、上传下载限速、重试次数、主题色、字体、缩放、接收目录、语言和启动选项。用户保存后，配置写入`settings.properties`，**【跳转 [Settings.java:L217-L225](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Settings.java#L217-L225) ✦ 讲解词：“当用户点击‘保存设置’时，我们读取控件最新的参数，触发 controller 写入 settings.properties 配置文件。随后立刻调用 render 函数，在不重启 App 的情况下动态更新全局样式。”】** 主窗口立即应用主题、字体和缩放；同时后端会更新局域网口令组摘要，**【跳转 [LocalBackend.java:L170-L173](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L170-L173) ✦ 讲解词：“LocalBackend.updateSettings 不仅保存设置，还会把当前用户局域网发现对象的 groupCode 口令组进行哈希更新，仅允许口令一致的小群组发现彼此，实现了私密局域网的安全隔离。”】** 让扫描和发现逻辑使用最新设置。

对应Java文件：`Settings.java`、`MainWindow.java`、`SystemSettings.java`、`AppController.java`、`LocalBackend.java`、`SettingsStore.java`、`AutoStart.java`、`LanPeer.java`。

---

### 8. 托盘、自启动与完成提示音流程：`tray.vsdx`

系统设置保存以后，还会影响一些系统级行为。开机自启动由`AutoStart.sync`负责，**【跳转 [AutoStart.java:L36-L50](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AutoStart.java#L36-L50) ✦ 讲解词：“在 Windows 平台下，如果勾选了开机自启动，AutoStart 会自动将运行 mvn/mvnw 的命令行指令以脚本形式写入系统的 Startup 目录，取消则直接删除脚本，无需侵入系统注册表。”】** 它会在Windows启动目录创建或删除`极速互传.cmd`。登录完成后，`MainWindow.applyStartupTray`会根据设置决定是否直接隐藏到托盘；托盘菜单提供显示窗口和退出App两个入口。**【跳转 [MainWindow.java:L421-L440](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L421-L440) ✦ 讲解词：“托盘由 ensureTray 承载。由于 JavaFX 没有原生的托盘接口，我们结合了 java.awt.SystemTray 与 AWT 菜单项实现了托盘托置和还原，保证了程序的后台持续服务能力。”】** 传输或接收完成时，如果用户开启了完成提示音，程序会调用`Toolkit.beep()`给出声音反馈。**【跳转 [MainWindow.java:L1237-L1245](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L1237-L1245) ✦ 讲解词：“当长耗时的传输完成，如果配置中的 soundOnComplete 为真，就会使用 AWT 级的 beep() 鸣叫提示，向用户提供直观的声音反馈。”】**

对应Java文件：`SettingsStore.java`、`AutoStart.java`、`Settings.java`、`MainWindow.java`、`SystemSettings.java`、`FileTransfer.java`、`UdpRx.java`。

---

### 9. 用户列表视图、搜索与分页流程：`userui.vsdx`

完成基础配置后，下一步是找到要发送的人。用户列表页面会同时加载局域网扫描到的设备和本地保存的分组，**【跳转 [UserList.java:L44-L52](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/UserList.java#L44-L52) ✦ 讲解词：“UserList 页面初始化时，使用了 CompletableFuture 的 thenCombine 动作，并发读取所有的发现设备和分组。两组异步任务都就绪后，才调度给 Platform.runLater 刷新列表，避免了 UI 的死锁卡顿。”】** 并提供矩阵视图和列表视图两种展示方式。矩阵视图按昵称和设备名搜索，并按15个用户一页分页；列表视图更适合看分组，支持按分组名和口令搜索。用户可以从这里直接发起发送，也可以展开分组查看成员，选中的对象会进入文件传输页的近期目标区。

对应Java文件：`UserList.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`UserDevice.java`、`Group.java`。

---

### 10. 局域网扫描流程：`scan.vsdx`

用户列表里的真实设备来源，是局域网扫描。用户点击扫描后，`UserList.scanUsers`保持在当前页面并显示扫描状态，然后调用`LocalBackend.scanLanDevices`进入`LanPeer.scan`。**【跳转 [LanPeer.java:L69-L88](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LanPeer.java#L69-L88) ✦ 讲解词：“局域网扫描是在 LanPeer.scan 中进行的。我们声明了 UDP 套接字，并启用其 Broadcast 属性，向包括 127.0.0.1、全网广播（255.255.255.255）和每块活动网卡计算出来的子网广播地址发送 DISCOVER 消息。”】** 扫描会向回环地址、全网广播地址和网卡广播地址发送`DISCOVER`；远端设备如果允许被发现，并且口令组摘要符合要求，就返回`HERE`。**【跳转 [LanPeer.java:L156-L180](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LanPeer.java#L156-L180) ✦ 讲解词：“被扫描端在后台的 replyLoop 线程监听发现端口。一旦收到包含口令摘要的 DISCOVER 请求，通过口令及隐身状态核对后，就打包编码自己的设备和状态信息以 UDP 单播包 HERE 应答回复。”】** 本机收到响应后记录设备信息和在线时间，再刷新用户列表。

对应Java文件：`UserList.java`、`AppController.java`、`LocalBackend.java`、`LanPeer.java`、`UserDevice.java`、`DeviceStatus.java`、`UserStatus.java`。

---

### 11. 用户分组流程：`group.vsdx`

扫描解决的是“找到单个设备”，分组解决的是“经常给一批人发送”。用户可以在矩阵视图里勾选真实用户，输入组名和默认口令后保存为分组；编辑已有分组时，也可以修改组名和口令。`GroupStore`会把成员快照写入`groups.properties`。**【跳转 [GroupStore.java:L33-L43](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/GroupStore.java#L33-L43) ✦ 讲解词：“在 GroupStore.save 中，对成员数据进行排重和格式化清洗后，将整个分组的信息写入 properties 配置文件，并在本地作为单独的分组虚拟设备进行存储。”】** 发送时，页面看到的是一个分组目标，真正进入后端发送前，再由`GroupStore.expand`把它展开成实际用户列表。**【跳转 [GroupStore.java:L72-L93](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/GroupStore.java#L72-L93) ✦ 讲解词：“发送前系统会调用 GroupStore.expand，它会在后台遍历所选目标，一旦遇到分组虚拟目标，即自动提取该组下的全部真实用户 ID 并去重展开，实现一键多播发送。”】**

对应Java文件：`UserList.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`GroupStore.java`、`Group.java`、`UserDevice.java`。

---

### 12. 近期对象与目标选择流程：`recent.vsdx`

除了现场扫描和手动分组，App还保留近期发送对象，方便重复传输。文件传输页打开时，`LocalBackend.loadRecentDevices`会先拿到本地分组目标，再加载`RecentStore`里的历史目标，并按ID去重合并。**【跳转 [LocalBackend.java:L57-L67](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L57-L67) ✦ 讲解词：“在这里，我们将本地保存的常驻分组设备与 RecentStore 从 properties 读出的历史传输目标加载到列表中，通过 ID 唯一性判断合并，确保近期联系人列表整洁不重复。”】** 用户可以在近期卡片上选中、取消选中或移除目标；一次传输完成后，`RecentStore.remember`会更新时间，并最多保留12个近期对象。**【跳转 [RecentStore.java:L58-L72](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/RecentStore.java#L58-L72) ✦ 讲解词：“传输动作触发后，页面会通知 RecentStore。我们更新这次发送的目标，如果是新联系人则置顶并写入文件，文件中的历史数量限制在 MAX_ITEMS 即最多 12 个。”】**

对应Java文件：`FileTransfer.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`RecentStore.java`、`GroupStore.java`、`UserDevice.java`。

---

### 13. 文件选择过滤与发送源展开流程：`fileprep.vsdx`

目标选好以后，还要准备要发送的文件。页面支持上传、拖拽和选择文件夹，前端先用`FileIcons.supported`按扩展名和文件夹类型判断是否允许，不支持的文件只计数提示，不进入发送队列。**【跳转 [FileIcons.java:L61-L69](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/util/FileIcons.java#L61-L69) ✦ 讲解词：“FileIcons.supported 设定了支持的文件拓展名过滤。拖拽时，如果是文件夹则直接允许，如果是文件，则必须落在预定义的文件类别后缀里才允许入队。”】** 真正发送前，`UdpTx.sources`会再次检查路径；如果是文件夹，就用`Files.walk`展开内部文件并过滤；**【跳转 [UdpTx.java:L545-L555](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L545-L555) ✦ 讲解词：“如果是选定文件夹，在 addDirectory 里我们利用 java.nio.file.Files.walk 递归获取底层的所有子文件，自动扁平化转换为以相对路径命名的 SourceFile 任务，以便批量发送。”】** 如果是普通文件，就再次按类型过滤，然后计算文件大小和SHA-256，作为后续完整性校验的依据。**【跳转 [UdpTx.java:L530-L543](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L530-L543) ✦ 讲解词：“在此处，我们不仅记录文件名称和大小，还会计算每个文件的 sha256 散列哈希值，作为接收端在接收完成后检验文件块是否缺失、损坏的终极判断基准。”】**

对应Java文件：`FileTransfer.java`、`FileIcons.java`、`TransferFile.java`、`UdpTx.java`。

---

### 14. 发送页面提提交口令与提交流程：`txui.vsdx`

当目标和文件都准备好以后，用户点击开始发送。`FileTransfer.startTransfer`会先做页面层校验：没有文件、没有目标或目标不可达时，只给出提示，不启动后端传输。校验通过后，页面弹出本次传输口令输入框，分组目标可以带出默认口令。**【跳转 [FileTransfer.java:L270-L309](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/FileTransfer.java#L270-L309) ✦ 讲解词：“在这里，前台检查选定文件和设备是否有空。如果条件满足，就弹出口令确认弹窗（输入或默认携带组口令），之后将切换到传输状态结果页，并在 Platform.runLater 的主线程进度刷新机制下启动后端传输异步任务。”】** 用户确认后，页面切换到结果页，并调用`AppController.startTransfer`启动后端任务；后续进度回调都通过`Platform.runLater`安全刷新JavaFX界面。

对应Java文件：`FileTransfer.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`UserDevice.java`、`TransferSummary.java`。

---

### 15. UDP发送准备流程：`txprep.vsdx`

请求进入后端后，并不会马上发分片，而是先做发送准备。`LocalBackend`用单线程队列串行执行传输任务，**【跳转 [LocalBackend.java:L18-L22](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L18-L22) ✦ 讲解词：“为杜绝多任务并发发送而引起本地物理网卡拥堵和丢包率急剧上升，LocalBackend 使用了名为 transferQueue 的 SingleThreadExecutor。这保证了即使前台多次提交传输，后端也能实现严格的串行发送。”】** 避免多个发送任务互相抢状态。后端会先用`GroupStore.expand`把分组目标展开成真实成员，再读取`SystemSettings`拿到限速、重试等配置。`UdpTx.run`随后展开源文件，并按上传限速和目标数量分配速率；对每个目标先发送`BEGIN`包，**【跳转 [UdpTx.java:L205-L216](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L205-L216) ✦ 讲解词：“在 sendFile 开始分片传输前，我们会调用 sendText 通过 UDP 发送 BEGIN 引导包，包含文件序号、大小、总片数、哈希以及口令，来让接收方完成初始化验证。”】** 里面包含文件名、大小、分片数、SHA-256和口令摘要。

对应Java文件：`LocalBackend.java`、`GroupStore.java`、`SettingsStore.java`、`SystemSettings.java`、`UdpTx.java`、`UserDevice.java`、`TransferFile.java`。

---

### 16. UDP分片ACK流程：`txchunk.vsdx`

接收端确认`BEGIN`以后，发送端才进入真正的数据分片阶段。`BEGIN ACK OK`里会带回接收端缺失的分片列表，所以这里天然支持断点续传。之后发送端最多使用8个worker并发发送分片，每个分片都要通过`sendWithAck`等待确认；**【跳转 [UdpTx.java:L241-L250](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L241-L250) ✦ 讲解词：“在 sendChunks 里，我们按缺失的分片分配发送工单，并采用多线程线程池模型（最大并发 8 个 worker 线程）同时发分片以跑满局域网带宽，且每个 worker 独立等待对应的 ACK 回应。”】** 期间会处理暂停、上传限速、失败重试和进度快照。**【跳转 [UdpTx.java:L380-L406](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L380-L406) ✦ 讲解词：“分片核心发送逻辑在 sendWithAck 中。每次尝试前都会检查 waitIfPaused 锁：若用户暂停，则线程在此阻塞；若网络状况差未收到 ACK，则根据重试次数进行指数级重发以保证传输可靠性。”】** 所有分片完成后，后端根据成功或失败结果生成对应的`TransferTask`。

对应Java文件：`UdpTx.java`、`UdpRx.java`、`TransferTask.java`、`TransferSummary.java`、`SystemSettings.java`。

---

### 17. 接收BEGIN流程：`rxbegin.vsdx`

从接收端看，第一步是处理发送端的`BEGIN`包。`UdpRx.listen`识别协议后，会解析job、文件序号、文件名、文件大小、SHA-256和口令摘要。如果这个文件之前已经接收过一部分，就直接返回缺失分片列表，让发送端续传；**【跳转 [UdpRx.java:L109-L122](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpRx.java#L109-L122) ✦ 讲解词：“在 handleBegin 中，对于新收到的 BEGIN 包，如果判定它属于之前因为掉线或暂停未接收完的文件，我们直接把本地 .part.meta 记录文件里缺失的文件块明细打包在 ACK 报文里发回，实现快速断点续传。”】** 如果是新文件，就检查文件类型、用户状态和传输口令。需要人工确认时，主窗口会弹出确认框或口令框；**【跳转 [MainWindow.java:L169-L180](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L169-L180) ✦ 讲解词：“如果当前状态是‘忙碌’或有传输口令，回调 confirmReceive 会在 UI 线程（使用 Platform.runLater）弹出口令或询问窗口，限制 30 秒超时时间，以保护接收方不受垃圾文件骚扰。”】** 确认通过后，接收端创建`.part`临时文件和`.meta`分片记录文件。

对应Java文件：`UdpRx.java`、`RxAsk.java`、`MainWindow.java`、`FileIcons.java`、`SettingsStore.java`、`UserStatus.java`。

---

### 18. 接收DATA流程：`rxdata.vsdx`

`BEGIN`通过后，接收端才开始处理真正的数据分片。`UdpRx.handleData`会解析jobId、fileIndex和chunkIndex，找到对应的接收文件后，按偏移写入`.part`文件，**【跳转 [UdpRx.java:L339-L367](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpRx.java#L339-L367) ✦ 讲解词：“在 RxFile.write 里，我们借助 NIO FileChannel 自由操作通道位置，根据分片序号计算出在临时文件中的具体写入偏移 chunkIndex * chunkSize，在无序接收状态下乱序落盘，大大提高了文件组装性能。”】** 并把已收到的分片写入`.meta`。文件没收齐时，接收端按进度节点回调界面；文件收齐后，必须重新计算SHA-256并和发送端声明的值比较。**【跳转 [UdpRx.java:L400-L413](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpRx.java#L400-L413) ✦ 讲解词：“当全部分片数收齐，finish 会立即校验文件的 sha256 指纹。若比对完全一致，则通过 Files.move 替换重命名为正式路径文件，并删除 .meta 历史记录以保障落盘文件的数据完整性。”】** 校验通过才会移动成最终文件并删除临时记录，校验失败则返回失败ACK。下载限速通过延迟ACK来实现。

对应Java文件：`UdpRx.java`、`RxProgress.java`、`SettingsStore.java`、`MainWindow.java`。

---

### 19. 传输结果页统计与清理流程：`result.vsdx`

最后回到用户能看到的结果页。无论传输正在进行还是已经结束，后端都会不断生成`TransferSummary`，页面根据它刷新统计卡片、任务列表和日志。**【跳转 [FileTransfer.java:L57-L66](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/FileTransfer.java#L57-L66) ✦ 讲解词：“结果展现完全是响应式数据驱动的。在 showTransferResultPage 中，界面依据当前 app.currentSummary 中的成功数、失败数、重试数以及具体的日志列表，实时刷新渲染整个 UI 控件树。”】** 用户可以清除已完成任务，程序会调用`TransferSummary.withoutCompleted`重新计算统计；也可以清空日志，调用`withoutLogs`保留任务但移除日志文本。**【跳转 [TransferSummary.java:L8-L15](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/model/TransferSummary.java#L8-L15) ✦ 讲解词：“如果用户选择清空日志，后台调用 withoutLogs 生成丢弃日志文本的新对象，这保持了配置与任务的无副作用处理，规避了状态并发竞争的安全漏洞。”】** 最终完成后，如果系统设置允许，就播放完成提示音，整个从登录、选人、选文件、发送、接收到结果展示的流程到这里闭环。

对应Java文件：`FileTransfer.java`、`MainWindow.java`、`TransferSummary.java`、`TransferTask.java`、`SystemSettings.java`。
