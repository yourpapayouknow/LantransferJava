## 简明讲稿与代码穿插指引

老师要求在讲解流程图时穿插展示对应的 Java 代码。为此，我们在本讲稿中，为 19 个流程讲解步骤补充了精细的 **“念完哪句后跳转到哪个文件的哪一行”** 指引以及对应的 **“穿插讲解词”**，帮助您在演示时无缝切换图表与代码。

---

### 1. App生命周期流程：`life.vsdx`

我们先从整个App的生命周期讲起。程序入口本身很轻，`App.main`只负责把进程交给JavaFX，真正的窗口、页面和后端初始化都从`MainWindow.start`开始。主窗口启动后，会先注册接收确认和进度回调，再创建`LocalBackend`，由后端启动UDP接收线程并加载系统设置。用户登录失败时会显示错误并停留在登录页，登录成功以后进入主界面；后续文件传输、用户列表、我的资料和系统设置都由侧边栏切换。最后用户关闭窗口或从托盘退出，进程才完成销毁。

对应Java文件：`App.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`SettingsStore.java`、`UdpRx.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“程序入口本身很轻，`App.main`只负责把进程交给JavaFX”
  - **跳转代码**：[App.java:L6-L8](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/App.java#L6-L8)
  - **讲解词**：“大家可以看到，本程序的启动入口非常简单，仅调用 `Application.launch(MainWindow.class, args)` 委托给 JavaFX 来加载并管理主窗口生命周期。”
- **在念完这句后**：“真正的窗口、页面和后端初始化都从`MainWindow.start`开始。主窗口启动后，会先注册接收确认和进度回调”
  - **跳转代码**：[MainWindow.java:L117-L131](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L117-L131)
  - **讲解词**：“在窗口启动后，通过 `controller.setRxAsk` 与 `controller.setRxProgress` 注册了底层的接收回调，以确保后端收到文件请求时能驱动 UI 弹窗，并随之异步载入用户首选的设置。”
- **在念完这句后**：“再创建`LocalBackend`，由后端启动UDP接收线程并加载系统设置。”
  - **跳转代码**：[LocalBackend.java:L25-L28](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L25-L28)
  - **讲解词**：“`LocalBackend` 实例化时，会立刻调用 `rx.start()`。这个方法会在后台开启独立的 UDP 监听线程，让 App 只要存活就能持续监听局域网的文件传输包。”
- **在念完这句后**：“用户登录失败时会显示错误并停留在登录页，登录成功以后进入主界面”
  - **跳转代码**：[Auth.java:L89-L97](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Auth.java#L89-L97)
  - **讲解词**：“在登录按钮动作中，若是验证通过，则更新主窗口的 `profile` 实例并调用 `showFileTransferPage()` 切入主功能页；若失败，直接弹出 Toast 气泡报错并不进行界面切换。”

---

### 2. 本地数据与配置存储流程：`storage.vsdx`

理解了App怎么启动之后，接下来要说明它的数据放在哪里。这个项目没有把运行数据随意散落在工程目录里，而是由`AppFiles.dataDir`统一决定数据目录：优先使用启动参数或环境变量，默认落在用户目录下的`.lantransfer`。设置、近期对象、分组和记住账号分别由各自的Store读写；账号主表仍然使用仓库根目录的`acco`，注册请求写入`req/`。最后这些来源都由`LocalBackend`组合起来，供页面统一调用。

对应Java文件：`AppFiles.java`、`SettingsStore.java`、`RecentStore.java`、`GroupStore.java`、`AuthStore.java`、`LocalBackend.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“优先使用启动参数或环境变量，默认落在用户目录下的`.lantransfer`。”
  - **跳转代码**：[AppFiles.java:L14-L20](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AppFiles.java#L14-L20)
  - **讲解词**：“`AppFiles.dataDir()` 实现了灵活的路径决定逻辑：如果检测到 `-Dlantransfer.dataDir` 参数或环境变量，就用重定向目录；默认会写入用户 Home 目录的 `.lantransfer` 下，并以仓库名作为子命名空间，避免不同作业实例混淆。”
- **在念完这句后**：“设置、近期对象、分组和记住账号分别由各自的Store读写”
  - **跳转代码**：[SettingsStore.java:L38-L48](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/SettingsStore.java#L38-L48)
  - **讲解词**：“我们以 `SettingsStore.load` 为例，它直接读取我们在数据目录下创建的 `settings.properties`。如果文件不存在就自动 fallback 返回默认值，读取过程采用 `synchronized` 确保线程安全。”

---

### 3. 核心数据模型承载关系：`model.vsdx`

有了数据落点以后，再看页面和后端之间传递的对象。页面层不直接操作文件、网络或账号表，而是通过`AppController`进入`BackendFacade`，再交给`LocalBackend`处理。登录结果、个人资料、系统设置、用户设备、分组、传输文件和传输结果都由明确的record或enum承载，这样页面只关心展示和事件，后端只关心业务执行。`MockBackendFacade`和`TxSim`只用于联调或自检，不属于当前正式主流程。

对应Java文件：`AppController.java`、`BackendFacade.java`、`LocalBackend.java`、`AuthResult.java`、`LoginRequest.java`、`RegisterRequest.java`、`Profile.java`、`SystemSettings.java`、`UserDevice.java`、`Group.java`、`TransferFile.java`、`TransferTask.java`、`TransferSummary.java`、`UserStatus.java`、`DeviceStatus.java`、`MockBackendFacade.java`、`TxSim.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“登录结果、个人资料、系统设置、用户设备、分组、传输文件和传输结果都由明确的record或enum承载”
  - **跳转代码**：[SystemSettings.java:L5-L10](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/model/SystemSettings.java#L5-L10)
  - **讲解词**：“在模型设计上，我们大量采用了 Java 17 的 `record` 关键字。比如 `SystemSettings`，它能够保证配置项的数据不可变性，并在线程间或 UI-服务层间传递时提供天然的数据一致性保障。”

---

### 4. 登录流程：`login.vsdx`

接下来进入用户真正开始使用App的第一步：登录。登录页会先从本机`la`里读取上次记住的账号，用户输入账号和密码后，页面把请求交给`AppController.login`，再进入`LocalBackend` 和 `AuthStore.login`。后端会读取`acco`账号表，用盐和PBKDF2摘要校验密码；校验成功后生成`Profile`，保存记住账号状态，并把当前用户同步给局域网发现和UDP接收模块。也就是说，登录不只是换到主界面，它同时建立了后面扫描、接收和发送所需的身份。

对应Java文件：`Auth.java`、`AppController.java`、`LocalBackend.java`、`AuthStore.java`、`LoginRequest.java`、`AuthResult.java`、`Profile.java`、`LanPeer.java`、`UdpRx.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“登录页会先从本机`la`里读取上次记住的账号”
  - **跳转代码**：[Auth.java:L80-L86](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Auth.java#L80-L86)
  - **讲解词**：“登录页面构建时，首先会从 `controller` 异步获取之前记住的账号名称并回填至文本框，自动勾选‘记住我’，提升用户的使用便利性。”
- **在念完这句后**：“后端会读取`acco`账号表，用盐和PBKDF2摘要校验密码”
  - **跳转代码**：[AuthStore.java:L65-L78](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L65-L78)
  - **讲解词**：“在 `AuthStore.login` 中，我们首先提取 `acco` properties，读取对应账号生成的随机密码盐和哈希结果，然后使用哈希方法进行核对。这种无服务器模式既轻量又保证了基础安全性。”
- **在念完这句后**：“用盐和PBKDF2摘要校验密码”
  - **跳转代码**：[AuthStore.java:L386-L394](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L386-L394)
  - **讲解词**：“具体校验算法位于 `hash` 函数中，我们基于 `SecretKeyFactory` 配合 `PBKDF2WithHmacSHA256` 算法进行 120,000 次加盐迭代哈希，极大增强了抗暴力破解与彩虹表攻击的能力。”

---

### 5. 注册流程：`reg.vsdx`

如果用户还没有账号，就走注册流程。注册页提交账号、密码和设备名后，页面会先禁用表单并显示处理中，防止重复提交。后端校验账号格式和重复账号，再生成盐和PBKDF2密码摘要，把注册请求写入`req/<账号>`。因为当前没有独立服务器，App通过运行时注入的`ACCO_T`或`-Dacco.t`把请求推送到GitHub仓库，等待Actions把请求合入`acco`；如果没有及时合入，用户会看到审核等待状态。

对应Java文件：`Auth.java`、`AppController.java`、`LocalBackend.java`、`AuthStore.java`、`RegisterRequest.java`、`AuthResult.java`、`Profile.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“注册页提交账号、密码和设备名后，页面会先禁用表单并显示处理中，防止重复提交。”
  - **跳转代码**：[Auth.java:L123-L128](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Auth.java#L123-L128)
  - **讲解词**：“为了防止用户在弱网环境下重复点击导致多次写仓库，当用户点击‘注册’时，我们会立刻将按钮与所有文本框调用 `setDisable(true)` 挂起，直到后端返回响应。”
- **在念完这句后**：“后端校验账号格式和重复账号，再生成盐和PBKDF2密码摘要，把注册请求写入`req/<账号>`。”
  - **跳转代码**：[AuthStore.java:L99-L114](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L99-L114)
  - **讲解词**：“在 `AuthStore.register` 中，先验证账号正则表达式，之后如果开启了 `gitSync` 自动同步，就把新用户信息和刚生成的盐加密哈希保存为注册请求的属性文件，存入待同步的 `req/` 目录。”
- **在念完这句后**：“App通过运行时注入的`ACCO_T`或`-Dacco.t`把请求推送到GitHub仓库”
  - **跳转代码**：[AuthStore.java:L531-L536](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AuthStore.java#L531-L536)
  - **讲解词**：“通过读取注入的 GitHub Token，构建出一个专用的 HTTPS 推送 URL，借由底层命令行工具安全推送到仓库。这样，GitHub Actions 就能监听到该 commit 并触发审批流，完美替代了重型的中央服务器。”

---

### 6. 我的资料流程：`prof.vsdx`

账号建立之后，用户可以在“我的资料”里维护自己的展示信息。`Mine`页面会先检查当前是否已有`Profile`，未登录就回到登录页；已登录时展示昵称、用户ID、设备名、签名和头像。头像上传后会用`ImageIO`裁剪压缩成128px JPEG，再转成Base64保存。资料和状态保存成功后，会写回账号数据，并同步给`LanPeer`广播和`UdpRx`接收门禁，这样别人扫描到的资料和接收端的状态判断都会更新。

对应Java文件：`Mine.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`AuthStore.java`、`Profile.java`、`UserStatus.java`、`LanPeer.java`、`UdpRx.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“头像上传后会用`ImageIO`裁剪压缩成128px JPEG，再转成Base64保存。”
  - **跳转代码**：[Mine.java:L132-L146](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Mine.java#L132-L146)
  - **讲解词**：“在 `jpegAvatar` 中，我们从用户的本地图片文件读取 `BufferedImage`，计算居中正方形裁剪区域，并缩放到标准 128*128 像素。这能够极大地减小头像的数据体积，确保广播时的低开销。”
- **在念完这句后**：“再转成Base64保存。资料和状态保存成功后，会写回账号数据”
  - **跳转代码**：[Mine.java:L147-L158](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Mine.java#L147-L158)
  - **讲解词**：“然后，我们调用 `ImageWriter` 将图像以较优质量（质量设为 0.72f）写进内存字节流，通过 Base64 编码为字符串，便于持久化和网络发现交互。”

---

### 7. 系统设置流程：`set.vsdx`

资料解决“我是谁”，系统设置解决“App怎么工作”。设置页会读取`SystemSettings`，展示本机IP、上传下载限速、重试次数、主题色、字体、缩放、接收目录、语言和启动选项。用户保存后，配置写入`settings.properties`，主窗口立即应用主题、字体和缩放；同时后端会更新局域网口令组摘要，让扫描和发现逻辑使用最新设置。

对应Java文件：`Settings.java`、`MainWindow.java`、`SystemSettings.java`、`AppController.java`、`LocalBackend.java`、`SettingsStore.java`、`AutoStart.java`、`LanPeer.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“用户保存后，配置写入`settings.properties`”
  - **跳转代码**：[Settings.java:L217-L225](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/Settings.java#L217-L225)
  - **讲解词**：“用户点击‘保存设置’后，页面页面构造出最新的 `SystemSettings` 对象并将其同步传回 `controller`。保存成功后立刻调用 `render` 函数使全局主题、显示字体比例等界面效果无缝刷新。”
- **在念完这句后**：“同时后端会更新局域网口令组摘要，让扫描和发现逻辑使用最新设置。”
  - **跳转代码**：[LocalBackend.java:L170-L173](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L170-L173)
  - **讲解词**：“在 `LocalBackend.updateSettings` 里，我们除了调用 Store 持久化配置之外，还会通过 `lan.updateGroup` 将用户的口令组做加密更新，防止未授权用户侦听到设备。”

---

### 8. 托盘、自启动与完成提示音流程：`tray.vsdx`

系统设置保存以后，还会影响一些系统级行为。开机自启动由`AutoStart.sync`负责，它会在Windows启动目录创建或删除`极速互传.cmd`。登录完成后，`MainWindow.applyStartupTray`会根据设置决定是否直接隐藏到托盘；托盘菜单提供显示窗口和退出App两个入口。传输或接收完成时，如果用户开启了完成提示音，程序会调用`Toolkit.beep()`给出声音反馈。

对应Java文件：`SettingsStore.java`、`AutoStart.java`、`Settings.java`、`MainWindow.java`、`SystemSettings.java`、`FileTransfer.java`、`UdpRx.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“开机自启动由`AutoStart.sync`负责，它会在Windows启动目录创建或删除`极速互传.cmd`。”
  - **跳转代码**：[AutoStart.java:L36-L50](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/AutoStart.java#L36-L50)
  - **讲解词**：“自启动的同步行为由 `AutoStart.sync` 控制：若勾选，则利用 `Files.writeString` 向 Windows Startup 目录下写入包含 `mvn javafx:run` 的启动脚本，若不勾选则直接物理删除该脚本，逻辑干净彻底。”
- **在念完这句后**：“托盘菜单提供显示窗口和退出App两个入口。”
  - **跳转代码**：[MainWindow.java:L421-L440](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L421-L440)
  - **讲解词**：“在 `ensureTray` 里我们初始化了托盘组件。因为 JavaFX 本身没有跨平台的系统托盘 API，我们通过 `java.awt.SystemTray` 与 `java.awt.TrayIcon` 构造了原生托盘右键菜单，并与显示及退出事件深度集成。”
- **在念完这句后**：“如果用户开启了完成提示音，程序会调用`Toolkit.beep()`给出声音反馈。”
  - **跳转代码**：[MainWindow.java:L1237-L1245](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L1237-L1245)
  - **讲解词**：“提示音控制在 `playDoneSound` 里实现，如果配置允许，直接通过 AWT 级的底层喇叭发出蜂鸣提示音，为已完成的长时后台传输任务提供实时反馈。”

---

### 9. 用户列表视图、搜索与分页流程：`userui.vsdx`

完成基础配置后，下一步是找到要发送的人。用户列表页面会同时加载局域网扫描到的设备和本地保存的分组，并提供矩阵视图和列表视图两种展示方式。矩阵视图按昵称和设备名搜索，并按15个用户一页分页；列表视图更适合看分组，支持按分组名和口令搜索。用户可以从这里直接发起发送，也可以展开分组查看成员，选中的对象会进入文件传输页的近期目标区。

对应Java文件：`UserList.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`UserDevice.java`、`Group.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“用户列表页面会同时加载局域网扫描到的设备和本地保存的分组”
  - **跳转代码**：[UserList.java:L44-L52](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/UserList.java#L44-L52)
  - **讲解词**：“加载时采用 `thenCombine` 高级异步编排技术，并发读取已知设备与本地分组，在两者数据均就绪后调用 `Platform.runLater` 安全将视图渲染到底层 JavaFX 主线程。”

---

### 10. 局域网扫描流程：`scan.vsdx`

用户列表里的真实设备来源，是局域网扫描。用户点击扫描后，`UserList.scanUsers`保持在当前页面并显示扫描状态，然后调用`LocalBackend.scanLanDevices`进入`LanPeer.scan`。扫描会向回环地址、全网广播地址和网卡广播地址发送`DISCOVER`；远端设备如果允许被发现，并且口令组摘要符合要求，就返回`HERE`。本机收到响应后记录设备信息和在线时间，再刷新用户列表。

对应Java文件：`UserList.java`、`AppController.java`、`LocalBackend.java`、`LanPeer.java`、`UserDevice.java`、`DeviceStatus.java`、`UserStatus.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“用户点击扫描后，`UserList.scanUsers`保持在当前页面并显示扫描状态”
  - **跳转代码**：[UserList.java:L148-L160](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/UserList.java#L148-L160)
  - **讲解词**：“当按下扫描按钮后，扫描逻辑在后台运行，界面通过刷新扫描状态来保持动画。运行结束后，再在主线程通过弹出 Toast 和再次拉取列表更新结果。”
- **在念完这句后**：“扫描会向回环地址、全网广播地址和网卡广播地址发送`DISCOVER`”
  - **跳转代码**：[LanPeer.java:L69-L88](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LanPeer.java#L69-L88)
  - **讲解词**：“扫描过程在 `LanPeer.scan` 中。我们为 `DatagramSocket` 设置了 200 毫秒的读取超时。在发送时，我们通过循环将带有群组标识的 `DISCOVER` 信息发往本地回环、全局广播地址以及从各块网卡获取到的特定子网广播地址，之后在循环体中读取回复。”
- **在念完这句后**：“远端设备如果允许被发现，并且口令组摘要符合要求，就返回`HERE`。”
  - **跳转代码**：[LanPeer.java:L156-L183](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LanPeer.java#L156-L183)
  - **讲解词**：“应答端会在后台线程 `replyLoop` 中监听扫描端口。一旦它接收到来自局域网的 `DISCOVER`，便会核对包含口令组的摘要与被发现状态；条件满足则向发起端单播发送包含自己头像、状态和主机地址的 `HERE` 报文。”

---

### 11. 用户分组流程：`group.vsdx`

扫描解决的是“找到单个设备”，分组解决的是“经常给一批人发送”。用户可以在矩阵视图里勾选真实用户，输入组名和默认口令后保存为分组；编辑已有分组时，也可以修改组名和口令。`GroupStore`会把成员快照写入`groups.properties`。发送时，页面看到的是一个分组目标，真正进入后端发送前，再由`GroupStore.expand`把它展开成实际用户列表。

对应Java文件：`UserList.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`GroupStore.java`、`Group.java`、`UserDevice.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“`GroupStore`会把成员快照写入`groups.properties`。”
  - **跳转代码**：[GroupStore.java:L33-L43](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/GroupStore.java#L33-L43)
  - **讲解词**：“在 `GroupStore.save` 中，我们将勾选的分组成员进行信息清洗，写到属性配置文件里，最后生成一个类型特殊的组对象目标返回给前台展示。”
- **在念完这句后**：“真正进入后端发送前，再由`GroupStore.expand`把它展开成实际用户列表。”
  - **跳转代码**：[GroupStore.java:L72-L93](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/GroupStore.java#L72-L93)
  - **讲解词**：“`expand` 函数实现了递归展开功能。它会遍历传入的发送目标，当发现是分组时，会自动查表解构出底下所有真实用户的 ID，并用哈希 Map 去重合并，最后在后端实现并发多播发送。”

---

### 12. 近期对象与目标选择流程：`recent.vsdx`

除了现场扫描和手动分组，App还保留近期发送对象，方便重复传输。文件传输页打开时，`LocalBackend.loadRecentDevices`会先拿到本地分组目标，再加载`RecentStore`里的历史目标，并按ID去重合并。用户可以在近期卡片上选中、取消选中或移除目标；一次传输完成后，`RecentStore.remember`会更新时间，并最多保留12个近期对象。

对应Java文件：`FileTransfer.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`RecentStore.java`、`GroupStore.java`、`UserDevice.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“`LocalBackend.loadRecentDevices`会先拿到本地分组目标，再加载`RecentStore`里的历史目标，并按ID去重合并。”
  - **跳转代码**：[LocalBackend.java:L57-L67](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L57-L67)
  - **讲解词**：“在后端异步获取近期对象时，我们将分组目标（`groups.targets()`）和近期历史发送目标（`recent.load()`）进行读取，并依 ID 判断是否存在重复，最终返回没有重复的目标卡片集合。”
- **在念完这句后**：“一次传输完成后，`RecentStore.remember`会更新时间，并最多保留12个近期对象。”
  - **跳转代码**：[RecentStore.java:L58-L72](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/RecentStore.java#L58-L72)
  - **讲解词**：“`RecentStore.remember` 负责更新最近联系人。我们先获取最新的目标卡片，并合并保留原有的历史记录。在此过程中，我们会将列表大小截断为 `MAX_ITEMS`（12 个），避免列表过于拥挤。”

---

### 13. 文件选择过滤与发送源展开流程：`fileprep.vsdx`

目标选好以后，还要准备要发送的文件。页面支持上传、拖拽和选择文件夹，前端先用`FileIcons.supported`按扩展名和文件夹类型判断是否允许，不支持的文件只计数提示，不进入发送队列。真正发送前，`UdpTx.sources`会再次检查路径；如果是文件夹，就用`Files.walk`展开内部文件并过滤；如果是普通文件，就再次按类型过滤，然后计算文件大小和SHA-256，作为后续完整性校验的依据。

对应Java文件：`FileTransfer.java`、`FileIcons.java`、`TransferFile.java`、`UdpTx.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“前端先用`FileIcons.supported`按扩展名和文件夹类型判断是否允许，不支持的文件只计数提示，不进入发送队列。”
  - **跳转代码**：[FileIcons.java:L61-L69](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/util/FileIcons.java#L61-L69)
  - **讲解词**：“`FileIcons.supported` 中定义了受支持的后缀名集合（例如 pdf、zip、doc、java、psd 等）。如果被拖拽或选中的文件不匹配这一列表，界面就会直接跳过此项，防止错误格式文件传输。”
- **在念完这句后**：“如果是文件夹，就用`Files.walk`展开内部文件并过滤”
  - **跳转代码**：[UdpTx.java:L545-L555](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L545-L555)
  - **讲解词**：“在 `addDirectory` 中，我们使用 Java NIO 的 `Files.walk` 递归深度遍历文件夹，把文件夹下的每一个支持格式的子文件取出。在这个过程中，我们将各文件的路径进行格式清洗，并放入实际文件列表中。”
- **在念完这句后**：“然后计算文件大小和SHA-256，作为后续完整性校验的依据。”
  - **跳转代码**：[UdpTx.java:L530-L543](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L530-L543)
  - **讲解词**：“在收集真实文件的核心方法中，我们通过 `sha256(path)` 计算每个文件对应的 SHA-256 值。该哈希值会写入开始协议包中，供接收端接收后校验是否损坏。”

---

### 14. 发送页面提交流程：`txui.vsdx`

当目标和文件都准备好以后，用户点击开始发送。`FileTransfer.startTransfer`会先做页面层校验：没有文件、没有目标或目标不可达时，只给出提示，不启动后端传输。校验通过后，页面弹出本次传输口令输入框，分组目标可以带出默认口令。用户确认后，页面切换到结果页，并调用`AppController.startTransfer`启动后端任务；后续进度回调都通过`Platform.runLater`安全刷新JavaFX界面。

对应Java文件：`FileTransfer.java`、`MainWindow.java`、`AppController.java`、`LocalBackend.java`、`UserDevice.java`、`TransferSummary.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“页面弹出本次传输口令输入框，分组目标可以带出默认口令。用户确认后，页面切换到结果页，并调用`AppController.startTransfer`启动后端任务；”
  - **跳转代码**：[FileTransfer.java:L270-L300](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/FileTransfer.java#L270-L300)
  - **讲解词**：“大家可以看到，`startTransfer` 首先检查文件和可用目标状态。若一切就绪，则调用口令确认对话框（`askTransferCode`），接着以回调的形式将带有 Platform.runLater 包装的进度显示接口传给后台开始传输。”

---

### 15. UDP发送准备流程：`txprep.vsdx`

请求进入后端后，并不会马上发分片，而是先做发送准备。`LocalBackend`用单线程队列串行执行传输任务，避免多个发送任务互相抢状态。后端会先用`GroupStore.expand`把分组目标展开成真实成员，再读取`SystemSettings`拿到限速、重试等配置。`UdpTx.run`随后展开源文件，并按上传限速和目标数量分配速率；对每个目标先发送`BEGIN`包，里面包含文件名、大小、分片数、SHA-256和口令摘要。

对应Java文件：`LocalBackend.java`、`GroupStore.java`、`SettingsStore.java`、`SystemSettings.java`、`UdpTx.java`、`UserDevice.java`、`TransferFile.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“`LocalBackend`用单线程队列串行执行传输任务，避免多个发送任务互相抢状态。”
  - **跳转代码**：[LocalBackend.java:L18-L22](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/LocalBackend.java#L18-L22)
  - **讲解词**：“在 `LocalBackend` 中，我们声明了一个非守护线程池 `transferQueue`，并特意指定它是单线程执行器（`SingleThreadExecutor`）。这保证了所有的发送请求在后端都会规整地排队，杜绝并发抢占网卡带宽而引起掉包。”
- **在念完这句后**：“对每个目标先发送`BEGIN`包，里面包含文件名、大小、分片数、SHA-256和口令摘要。”
  - **跳转代码**：[UdpTx.java:L205-L216](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L205-L216)
  - **讲解词**：“在 `sendFile` 的开头，我们先执行了 `sendText` 方法向对方发出带有 `BEGIN` 字样的元数据包（内含分片大小、哈希、口令组信息等）。这可以询问接收方是否同意接收、是否存在旧分片能断点续传。”

---

### 16. UDP分片ACK流程：`txchunk.vsdx`

接收端确认`BEGIN`以后，发送端才进入真正的数据分片阶段。`BEGIN ACK OK`里会带回接收端缺失的分片列表，所以这里天然支持断点续传。之后发送端最多使用8个worker并发发送分片，每个分片都要通过`sendWithAck`等待确认；期间会处理暂停、上传限速、失败重试和进度快照。所有分片完成后，后端根据成功或失败结果生成对应的`TransferTask`。

对应Java文件：`UdpTx.java`、`UdpRx.java`、`TransferTask.java`、`TransferSummary.java`、`SystemSettings.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“之后发送端最多使用8个worker并发发送分片，每个分片都要通过`sendWithAck`等待确认；”
  - **跳转代码**：[UdpTx.java:L241-L255](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L241-L255)
  - **讲解词**：“为了充分跑满千兆网卡，在 `sendChunks` 中我们会按照文件分片数动态设定最大 8 个并发的 worker 线程（使用线程池执行），各 worker 线程在分配好的段中分别执行发送并等待 ACK 应答。”
- **在念完这句后**：“每个分片都要通过`sendWithAck`等待确认；期间会处理暂停、上传限速、失败重试和进度快照。”
  - **跳转代码**：[UdpTx.java:L380-L405](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpTx.java#L380-L405)
  - **讲解词**：“在 `sendWithAck` 的核心重试循环中，如果监测到用户按下了页面上的‘暂停’按钮（通过 `waitIfPaused` 逻辑），发送线程会被同步锁阻塞，直到继续发送。发送失败会在配置的 retry 限额内尝试多次重发。”

---

### 17. 接收BEGIN流程：`rxbegin.vsdx`

从接收端看，第一步是处理发送端的`BEGIN`包。`UdpRx.listen`识别协议后，会解析job、文件序号、文件名、文件大小、SHA-256 and 口令摘要。如果这个文件之前已经接收过一部分，就直接返回缺失分片列表，让发送端续传；如果是新文件，就检查文件类型、用户状态和传输口令。需要人工确认时，主窗口会弹出确认框或口令框；确认通过后，接收端创建`.part`临时文件和`.meta`分片记录文件。

对应Java文件：`UdpRx.java`、`RxAsk.java`、`MainWindow.java`、`FileIcons.java`、`SettingsStore.java`、`UserStatus.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“如果这个文件之前已经接收过一部分，就直接返回缺失分片列表，让发送端续传”
  - **跳转代码**：[UdpRx.java:L109-L122](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpRx.java#L109-L122)
  - **讲解词**：“在 `handleBegin` 里，我们首先检查当前内存中是否已有该任务标识的接收对象。如果已经有相同的任务了，就会将已收到的分片元数据列表打包发回给发送方，使其自动识别并开始断点续传。”
- **在念完这句后**：“需要人工确认时，主窗口会弹出确认框或口令框；”
  - **跳转代码**：[MainWindow.java:L169-L180](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/MainWindow.java#L169-L180)
  - **讲解词**：“我们在主窗口中实现了解耦的接收确认方法。如果当前线程非 JavaFX 渲染主线程，我们使用 `CompletableFuture` 配合 `Platform.runLater` 将请求调度给 UI，主线程弹窗等待 30 秒，如果超时没有确认则按拒绝返回。”

---

### 18. 接收DATA流程：`rxdata.vsdx`

`BEGIN`通过后，接收端才开始处理真正的数据分片。`UdpRx.handleData`会解析jobId、fileIndex和chunkIndex，找到对应的接收文件后，按偏移写入`.part`文件，并把已收到的分片写入`.meta`。文件没收齐时，接收端按进度节点回调界面；文件收齐后，必须重新计算SHA-256并和发送端声明的值比较。校验通过才会移动成最终文件并删除临时记录，校验失败则返回失败ACK。下载限速通过延迟ACK来实现。

对应Java文件：`UdpRx.java`、`RxProgress.java`、`SettingsStore.java`、`MainWindow.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“找到对应的接收文件后，按偏移写入`.part`文件，并把已收到的分片写入`.meta`。”
  - **跳转代码**：[UdpRx.java:L339-L367](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpRx.java#L339-L367)
  - **讲解词**：“在 `RxFile.write` 写入方法中，我们根据当前分片的序号定位到分片应该在的磁盘偏移位置 `chunkIndex * chunkSize`，将数据直接写入 `.part` 中并将其加入元数据列表中。这种通过随机通道分片写入的设计让并发接收能跑得极快。”
- **在念完这句后**：“文件收齐后，必须重新计算SHA-256并和发送端声明的值比较。校验通过才会移动成最终文件并删除临时记录”
  - **跳转代码**：[UdpRx.java:L400-L413](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/service/UdpRx.java#L400-L413)
  - **讲解词**：“在 `finish` 方法中，接收端会使用 `sha256(part)` 计算接收到临时文件的最终散列，与发送端声明的哈希比对。若相等则调用 `Files.move` 替换重命名为真实目标文件，最后物理删除 `.part.meta` 文件以保障文件的绝对完整。”

---

### 19. 传输结果页统计与清理流程：`result.vsdx`

最后回到用户能看到的结果页。无论传输正在进行还是已经结束，后端都会不断生成`TransferSummary`，页面根据它刷新统计卡片、任务列表和日志。用户可以清除已完成任务，程序会调用`TransferSummary.withoutCompleted`重新计算统计；也可以清空日志，调用`withoutLogs`保留任务但移除日志文本。最终完成后，如果系统设置允许，就播放完成提示音，整个从登录、选人、选文件、发送、接收到结果展示的流程到这里闭环。

对应Java文件：`FileTransfer.java`、`MainWindow.java`、`TransferSummary.java`、`TransferTask.java`、`SystemSettings.java`。

**【穿插代码讲解步骤】**
- **在念完这句后**：“无论传输正在进行还是已经结束，后端都会不断生成`TransferSummary`，页面根据它刷新统计卡片、任务列表和日志。”
  - **跳转代码**：[FileTransfer.java:L57-L66](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/view/FileTransfer.java#L57-L66)
  - **讲解词**：“前台的 `showTransferResultPage` 实际上是一个完全数据驱动的逻辑。只要 `app.currentSummary` 有所变动，整个界面的上传控制栏、统计数字栏和日志区域就会完全跟着联动渲染刷新。”
- **在念完这句后**：“也可以清空日志，调用`withoutLogs`保留任务但移除日志文本。”
  - **跳转代码**：[TransferSummary.java:L12-L15](file:///F:/College/Activ_CodeProjects/IdeaProjects/Class_JiSuanJiWangLuoDaZuoYe/src/main/java/com/iwmei/lantransfer/model/TransferSummary.java#L12-L15)
  - **讲解词**：“在 `TransferSummary.withoutLogs` 里，我们简单生成一个把 logs 列表设为空的不可变对象。这种不可变模型重构极大地减少了多线程进度更新时的边界状态竞争问题。”
