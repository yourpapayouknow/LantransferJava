# 未实现功能清单0

本文档只记录当前仍未真正实现、仍是占位、演示兜底或仅保存配置但没有业务闭环的功能。

## 传输内核

1. 真实 UDP 多线程传输调度与实时进度
   - 当前状态：部分未实现。`UdpTx` 和 `UdpRx` 已实现目标级并发 UDP 文件发送、停止等待 ACK、后台接收和接收目录落盘；`LocalBackend` 已让多个传输请求进入单线程 FIFO 队列；`UdpTx` 已在发送端按进度节点推送传输中快照，`FileTransfer` 会实时刷新发送进度；`UdpRx` 已在接收端按进度节点推送回调，`MainWindow` 会用 toast 展示接收进度。
   - 跳过项：单文件分片级并发。
   - 跳过原因：当前 `UdpTx/UdpRx` 使用停止等待 ACK，已经具备确认、重试、断点补发、完整性校验和进度展示；直接改分片级并发需要滑动窗口、乱序缓存、窗口确认和批量缺失重传，代码量和协议风险明显上升，不适合在当前效率优先阶段硬做。
   - 当前占位方式：继续使用目标级并发和单目标内停止等待传输；多个目标可以并发，单个文件分片按顺序确认。
   - 推荐替代方案：只有在大文件实测吞吐不足时，再增加窗口大小配置，并实现“发送窗口 + 接收乱序缓存 + 累计 ACK/缺失列表 + 窗口级重传”。

## 传输协作能力

当前暂无传输协作能力的未实现核心项。
